name: amt-eval
channels:
    - defaults
    - conda-forge
    - pytorch
    - nvidia
dependencies:
    - librosa
    - pandas
    - python==3.10
    - pip
    - pytest
    - pytorch
    - torchvision
    - torchaudio
    - tqdm
    - pytorch-cuda=12.4
    - pretty_midi
    - pip:
        - mir-eval
        - soundfile==0.12.1
prefix: /home/mpk/anaconda3/envs/amt-eval